---
phase: 08-game-mode
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [src/systems/BombSystem.js, src/systems/RoundSystem.js, src/systems/BombSystem.test.js]
autonomous: true
requirements: [GAME-01]
user_setup: []
must_haves:
  truths:
    - "Holding 'B' near a planted bomb starts defusing"
    - "Defusing is twice as fast with a defuse kit"
    - "Round ends and resets after bomb defusal or explosion"
    - "Players are teleported to spawn points on round reset"
  artifacts:
    - path: "src/systems/BombSystem.js"
      provides: "Hold-B defusal logic"
    - path: "src/systems/RoundSystem.js"
      provides: "Round win conditions and reset teleportation"
  key_links:
    - from: "src/systems/BombSystem.js"
      to: "src/systems/RoundSystem.js"
      via: "this.engine.emit('bomb:defused')"
    - from: "src/systems/RoundSystem.js"
      to: "src/systems/PlayerControllerSystem.js"
      via: "engine.emit('round:reset')"
---

<objective>
Implement Bomb Defusal and Round Reset.
This wave adds the core interaction of defusing the bomb (holding 'B') and handles the cleanup and teleportation required to start a new round.
</objective>

<execution_context>
@/home/jaymes/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-game-mode/08-CONTEXT.md
@.planning/phases/08-game-mode/08-RESEARCH.md
@.planning/phases/08-game-mode/08-VALIDATION.md
@src/systems/BombSystem.js
@src/systems/RoundSystem.js
@src/GameState.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Hold-B Defusal in BombSystem</name>
  <files>src/systems/BombSystem.js, src/systems/BombSystem.test.js</files>
  <action>
    1. Update `src/systems/BombSystem.js`:
       - Add proximity check: Determine if player is within `DEFUSE_RADIUS` of `plantedBomb`.
       - Implement hold logic: Check `InputSystem.isKeyPressed('KeyB')`.
       - Track `defuseProgress` (0 to 1) in `GameState`.
       - Defusal time: 10s base, 5s if `GameState.get('hasDefuseKit')` is true.
       - Emit `bomb:defused` when progress reaches 1.
       - Reset progress if 'B' is released or player moves away.
    2. Add unit tests to `src/systems/BombSystem.test.js` to verify:
       - Defusal starts on 'B' hold near bomb.
       - Progress correctly tracks over time.
       - Kit detection correctly halves the time.
       - Releasing 'B' cancels defusal.
  </action>
  <verify>
    <automated>npm test src/systems/BombSystem.test.js</automated>
  </verify>
  <done>Bomb defusal is functional with 'B' hold interaction and kit support.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Round End, Win Conditions, and Reset Teleportation</name>
  <files>src/systems/RoundSystem.js, src/systems/PlayerControllerSystem.js</files>
  <action>
    1. Update `src/systems/RoundSystem.js`:
       - Listen for `bomb:defused` (CT win), `bomb:exploded` (T win).
       - In `ROUND_RUNNING`, if timer hits 0 before plant, CT wins.
       - On win, transition to `POST_ROUND`, update `teamScores`, and wait 5 seconds.
       - Implement `resetRound()`:
         - Teleport local player and bots to their respective spawn points (defined in `Maps_v2.js`).
         - Reset health, ammo, and clear temporary scene objects (grenades, planted bomb).
         - Emit `round:reset` for other systems to clean up.
         - Transition back to `PREROUND`.
    2. Update `src/systems/PlayerControllerSystem.js`:
       - Listen for `round:reset` and reset velocity/state.
  </action>
  <verify>
    <automated>npm test src/systems/RoundSystem.test.js</automated>
  </verify>
  <done>Full round cycle (Start -> Play -> End -> Reset) is complete with teleportation.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Competitive Bomb Defusal Loop</what-built>
  <how-to-verify>
    1. Start a match.
    2. Wait for 15s freeze time (verify movement is locked).
    3. Plant the bomb as a Terrorist.
    4. Attempt to defuse as a Counter-Terrorist by holding 'B' (verify progress bar in UI).
    5. Allow the bomb to explode or defuse it.
    6. Verify the round ends, scores update, and after 5s you are teleported back to spawn with a new freeze time.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Manual playtest of the full round cycle from PREROUND through ROUND_RUNNING to POST_ROUND and back to PREROUND.
</verification>

<success_criteria>
- Bomb can be defused by holding 'B' near it.
- Round ends on defusal, explosion, or time out.
- Scores are tracked correctly across rounds.
- Players teleport to spawn points when a new round starts.
- Game loop continues indefinitely.
</success_criteria>

<output>
After completion, create `.planning/phases/08-game-mode/08-02-SUMMARY.md`
</output>
