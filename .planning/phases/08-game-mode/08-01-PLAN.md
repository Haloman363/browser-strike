---
phase: 08-game-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/GameState.js, src/systems/RoundSystem.js, main.js, src/systems/PlayerControllerSystem.js, src/GameState.test.js]
autonomous: true
requirements: [GAME-01]
must_haves:
  truths:
    - "Game starts in PREROUND (Freeze Time)"
    - "Player cannot move during PREROUND"
    - "Round transitions to ROUND_RUNNING after 15 seconds"
  artifacts:
    - path: "src/systems/RoundSystem.js"
      provides: "Round Finite State Machine"
    - path: "src/GameState.test.js"
      provides: "Unit tests for round transitions"
  key_links:
    - from: "src/systems/RoundSystem.js"
      to: "src/GameState.js"
      via: "GameState.set({ roundState: ... })"
    - from: "src/systems/PlayerControllerSystem.js"
      to: "src/GameState.js"
      via: "GameState.get('roundState')"
---

<objective>
Implement the Round Finite State Machine (FSM) and Movement Locking.
This wave establishes the foundation for competitive rounds, including the 15-second freeze time (PREROUND) and active round state (ROUND_RUNNING).
</objective>

<execution_context>
@/home/jaymes/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-game-mode/08-CONTEXT.md
@.planning/phases/08-game-mode/08-RESEARCH.md
@.planning/phases/08-game-mode/08-VALIDATION.md
@src/GameState.js
@src/systems/PlayerControllerSystem.js
@main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Round FSM in GameState and create RoundSystem</name>
  <files>src/GameState.js, src/systems/RoundSystem.js, main.js</files>
  <action>
    1. Update `src/GameState.js`: Add `roundState` (default 'PREROUND'), `roundTimeLeft` (default 15), `teamScores` ({A: 0, B: 0}), and `hasDefuseKit` (default false) to the initial state.
    2. Create `src/systems/RoundSystem.js`:
       - Implement `RoundSystem` extending `System`.
       - Manage states: `PREROUND` (15s), `ROUND_RUNNING` (115s), `POST_ROUND` (5s).
       - Host-authoritative logic: If `GameState.get('isHost')` is true, decrement `roundTimeLeft` and transition states.
       - Broadcast `ROUND_STATE` (state + timer) via `NetworkSystem` if implemented (or prepare for it).
    3. Register `RoundSystem` in `main.js` using `engine.registerSystem(RoundSystem)`.
  </action>
  <verify>
    <automated>npm test src/GameState.test.js</automated>
  </verify>
  <done>RoundSystem exists and manages transitions between PREROUND and ROUND_RUNNING.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Movement Locking during PREROUND and POST_ROUND</name>
  <files>src/systems/PlayerControllerSystem.js</files>
  <action>
    1. Modify `src/systems/PlayerControllerSystem.js`:
       - In `handleKeyDown` and `applyInput`, check if `GameState.get('roundState')` is 'PREROUND' or 'POST_ROUND'.
       - If in a locked state, prevent movement input from updating velocity or position.
       - Ensure players can still look around (mouse movement), but horizontal and vertical movement is disabled.
    2. Ensure that buy menu interactions (Phase 9) would still be allowed during `PREROUND` (don't lock all input, just movement).
  </action>
  <verify>
    <automated>npm test src/systems/PlayerControllerSystem.test.js</automated>
  </verify>
  <done>Player movement is restricted during freeze time and round-end phases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create GameState and RoundSystem Tests</name>
  <files>src/GameState.test.js, src/systems/RoundSystem.test.js</files>
  <behavior>
    - Test that `GameState` initializes with correct round defaults.
    - Test that `RoundSystem` transitions from `PREROUND` to `ROUND_RUNNING` after the timer reaches zero.
    - Test that `RoundSystem` updates `GameState` with the correct remaining time.
  </behavior>
  <action>
    Create unit tests using Vitest to verify the state transitions and timer logic implemented in Tasks 1 and 2.
  </action>
  <verify>
    <automated>npm test src/GameState.test.js src/systems/RoundSystem.test.js</automated>
  </verify>
  <done>Automated tests confirm FSM logic is robust and correct.</done>
</task>

</tasks>

<verification>
Check that the game starts in PREROUND with a 15-second timer. Verify movement is locked. After 15 seconds, verify state changes to ROUND_RUNNING and movement is unlocked.
</verification>

<success_criteria>
- GameState correctly tracks round status.
- RoundSystem manages time-based transitions autonomously (on Host).
- Movement is correctly locked/unlocked based on round state.
- All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/08-game-mode/08-01-SUMMARY.md`
</output>
