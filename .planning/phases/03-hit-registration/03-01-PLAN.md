---
phase: 03-hit-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/systems/NetworkSystem.js, src/systems/NetworkSystem.test.js]
autonomous: true
requirements: [NET-04]

must_haves:
  truths:
    - "Host maintains a 200ms historical vault of player states"
    - "Client and Host clocks are synchronized via NTP-style offset"
    - "Snapshots include crouching state for accurate hitboxes"
  artifacts:
    - path: "src/systems/NetworkSystem.js"
      provides: "Clock sync and State Vaulting"
    - path: "src/systems/NetworkSystem.test.js"
      provides: "Automated verification for networking core"
  key_links:
    - from: "NetworkSystem.init"
      to: "SnapshotInterpolation.vault"
      via: "Configuration"
    - from: "NetworkSystem._handleMessage('TIME_SYNC')"
      to: "NetworkSystem.send('TIME_ACK')"
      via: "NTP algorithm"
---

<objective>
Implement the foundational networking logic for Phase 3: Clock Synchronization and State Vaulting.
This provides the host with the historical data needed to perform lag compensation (rewind) during hit registration.
</objective>

<execution_context>
@/home/jaymes/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-hit-registration/03-CONTEXT.md
@.planning/phases/03-hit-registration/03-RESEARCH.md
@.planning/phases/03-hit-registration/03-VALIDATION.md
@src/systems/NetworkSystem.js
@src/systems/NetworkSystem.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement NTP-style Clock Synchronization</name>
  <files>src/systems/NetworkSystem.js</files>
  <action>
    Implement an NTP-style clock synchronization algorithm in `NetworkSystem.js`.
    1. Add `TIME_SYNC` message handling on the host: Receive `{ t1 }`, reply with `TIME_ACK` containing `{ t1, hostTime: performance.now() }`.
    2. Add `TIME_ACK` message handling on the client: Calculate RTT (`t2 - t1`) and offset `((hostTime + rtt/2) - t2)`.
    3. Expose `getServerTime()` method that returns `performance.now() + this.clockOffset`.
    4. Implement a periodic sync (e.g., every 5 seconds) to maintain accuracy.
  </action>
  <verify>
    <automated>npm test src/systems/NetworkSystem.test.js</automated>
  </verify>
  <done>Client `getServerTime()` returns a value closely matching Host `performance.now()` after handshake.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Host-side State Vaulting</name>
  <files>src/systems/NetworkSystem.js</files>
  <action>
    Configure the host to maintain a historical record of all player states.
    1. Update `broadcastSnapshot` to include `isCrouched` state for each player entity.
    2. On the Host, save every created snapshot into `this.SI.vault` using `this.SI.vault.add(snapshot)`.
    3. Ensure the vault is configured to hold at least 200ms of history (SnapshotInterpolation default is usually 1000ms, which is fine).
    4. Update `NetworkSystem.test.js` to verify that `isCrouched` is present in snapshots and that snapshots are added to the vault on the host.
  </action>
  <verify>
    <automated>npm test src/systems/NetworkSystem.test.js</automated>
  </verify>
  <done>Host `SI.vault` contains recent snapshots including crouching status of all players.</done>
</task>

</tasks>

<verification>
1. Run `npm test src/systems/NetworkSystem.test.js` to ensure clock sync and vaulting logic pass unit tests.
2. Manually verify in a 2-tab local session that the "offset" stabilizes after the initial handshake.
</verification>

<success_criteria>
- NTP clock offset is calculated and applied on the client.
- Host maintains a history of snapshots in its vault.
- Snapshots accurately reflect player crouch state.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hit-registration/03-01-SUMMARY.md`
</output>
