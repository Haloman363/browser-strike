---
phase: 03-hit-registration
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/systems/WeaponSystem.js, src/systems/WeaponSystem.test.js, src/systems/NetworkSystem.js, main.js]
autonomous: false
requirements: [NET-04]

must_haves:
  truths:
    - "Shooting a moving target with 200ms lag results in a hit if aligned on shooter's screen"
    - "Blood splatters appear only after host confirmation"
    - "Host Authoritative hit detection prevents client-side 'god mode' hacks"
  artifacts:
    - path: "src/systems/WeaponSystem.js"
      provides: "SHOOT_REQUEST logic and Hit Feedback handling"
    - path: "src/systems/WeaponSystem.test.js"
      provides: "Integration tests for shooting and rewinding"
  key_links:
    - from: "WeaponSystem.shoot"
      to: "NetworkSystem.send('SHOOT')"
      via: "Timestamp-based request"
    - from: "NetworkSystem._handleMessage('SHOOT')"
      to: "Vault.get(timestamp)"
      via: "Lag compensation rewind"
    - from: "NetworkSystem.send('HIT_CONFIRMED')"
      to: "WeaponSystem.handleHitConfirmed"
      via: "Confirmation broadcast"
---

<objective>
Implement the Authoritative Hit Registration system, including lag compensation (backtracking) and host-side validation.
This ensures that "what you see is what you hit" for shooters, while maintaining competitive integrity through server-side truth.
</objective>

<execution_context>
@/home/jaymes/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-hit-registration/03-CONTEXT.md
@.planning/phases/03-hit-registration/03-RESEARCH.md
@.planning/phases/03-hit-registration/03-VALIDATION.md
@src/systems/WeaponSystem.js
@src/systems/NetworkSystem.js
@main.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Authoritative SHOOT flow and Test Scaffold</name>
  <files>src/systems/WeaponSystem.test.js, src/systems/WeaponSystem.js</files>
  <behavior>
    - Test that `WeaponSystem.shoot()` sends a `SHOOT` message to the host with a server-synced timestamp.
    - Test that the client DOES NOT predict blood splatters upon firing.
    - Test that receiving a `HIT_CONFIRMED` message triggers a blood splatter on the shooter.
  </behavior>
  <action>
    Create the `WeaponSystem.test.js` scaffold and refactor `WeaponSystem.js`.
    1. Modify `WeaponSystem.shoot()` to call `NetworkSystem.getServerTime()` and send a `SHOOT` command containing `{ timestamp, origin, direction, weaponKey }`.
    2. Remove the direct call to `handleHit` (and blood splatter logic) from the client's `shoot()` function.
    3. Add a `HIT_CONFIRMED` message handler to `NetworkSystem.js` that emits a 'weapon:hit_confirmed' event.
    4. In `WeaponSystem.js`, listen for 'weapon:hit_confirmed' and call `handleHitConfirmed(data)` to render blood/impact visuals.
  </action>
  <verify>
    <automated>npm test src/systems/WeaponSystem.test.js</automated>
  </verify>
  <done>Shooting flow is transitioned from client-authoritative to request-confirmation.</done>
</task>

<task type="auto">
  <name>Task 2: Host-side Backtracking and Validation</name>
  <files>src/systems/NetworkSystem.js</files>
  <action>
    Implement the rewind-check-restore logic on the host.
    1. In `NetworkSystem.js`, handle the `SHOOT` message (if host).
    2. Extract the `timestamp` from the message.
    3. Retrieve the historical state from the vault: `const pastState = this.SI.vault.get(timestamp)`.
    4. **Rewind**: Temporarily move all relevant networked entities to their positions/rotations/crouch states in `pastState`.
    5. **Raycast**: Perform an authoritative `THREE.Raycaster` check against these rewound entities using the provided origin and direction.
    6. **Restore**: Immediately move entities back to their "current" authoritative positions.
    7. If a hit is detected, broadcast `HIT_CONFIRMED` with `{ targetId, damage, point, isHeadshot }`.
  </action>
  <verify>
    <automated>npm test src/systems/WeaponSystem.test.js</automated>
  </verify>
  <done>Host accurately validates shots against historical world states matching the shooter's perceived view.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Authoritative Lag Compensation and Hit Confirmation</what-built>
  <how-to-verify>
    1. Open two browser tabs (Host and Client).
    2. Simulate 200ms latency on the Client (e.g., using Chrome DevTools Network conditions or a manual delay in `NetworkSystem.js` for testing).
    3. As the Host player, move side-to-side.
    4. As the Client player, aim and shoot at where the Host IS currently on the screen.
    5. Verify: Hit registers, blood splatters appear on BOTH screens after a small delay.
    6. Verify: Shooting behind where the host was on the client's screen DOES NOT register a hit.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- Host-side raycasting accurately detects hits on rewound hitboxes.
- Hits are rejected if the shooter aims at an old position (outside the 200ms window).
- All tests in `src/systems/WeaponSystem.test.js` pass.
</verification>

<success_criteria>
- NET-04: Accurate hit registration for pings up to 200ms.
- Confirmed hit feedback (blood only appears on confirmation).
- Authoritative damage application on the Host.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hit-registration/03-02-SUMMARY.md`
</output>
