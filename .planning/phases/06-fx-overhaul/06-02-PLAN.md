---
phase: 06-fx-overhaul
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [src/systems/FXSystem.js]
autonomous: true
requirements: [WGPU-04]
must_haves:
  truths:
    - "Smoke expands into a dense volumetric cloud over time"
    - "Smoke edges blend softly with scene geometry (no hard clipping)"
    - "Smoke particles fade out at the end of their life cycle"
  artifacts:
    - path: "src/systems/FXSystem.js"
      provides: "Smoke compute nodes and soft-particle material logic"
  key_links:
    - from: "src/systems/FXSystem.js"
      to: "Depth sampling"
      via: "TSL depth node"
    - from: "NetworkSystem"
      to: "FXSystem"
      via: "detonation event listener"
---

<objective>
Implement dense, performant volumetric-style smoke particles using TSL compute simulation and soft-particle depth blending.

Purpose: Achieve high-fidelity smoke effects that interact correctly with the environment without the performance cost of traditional CPU-based particles.
Output: A volumetric smoke system that can be triggered by grenade detonation events.
</objective>

<execution_context>
@/home/jaymes/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-fx-overhaul/06-CONTEXT.md
@.planning/phases/06-fx-overhaul/06-RESEARCH.md
@src/systems/FXSystem.js (Created in Plan 01)
@src/systems/NetworkSystem.js

<interfaces>
From src/systems/FXSystem.js (Planned in Plan 01):
```javascript
// Central FX management with WebGPU compute dispatch
```

From Research (Soft Particle TSL):
```javascript
const smokeAlpha = Fn(() => {
    const screenDepth = depth; // Viewport depth
    const particleDepth = viewportSafeDepth; // Current fragment depth
    const diff = screenDepth.sub(particleDepth);
    const softFactor = diff.smoothstep(0.0, 0.1);
    return texColor.a.mul(softFactor).mul(ageFade);
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TSL Volumetric Smoke Simulation</name>
  <files>src/systems/FXSystem.js</files>
  <action>
    1. Extend `FXSystem` to include a second `StorageBuffer` for smoke particles (max 5,000 per smoke grenade).
    2. Implement TSL `compute()` nodes for smoke:
       - `computeSmokeSpawn`: Sets initial position, randomized low-velocity drift, and long life duration.
       - `computeSmokeUpdate`: Simulates expanding bloom (position += velocity * drift), adds turbulence using `mx_noise`, and handles slow expansion (scaling over time).
    3. Update `FXSystem.update()` to dispatch smoke compute updates.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>TSL compute shaders for smoke expansion and drift are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Soft Particle Blending & Depth Integration</name>
  <files>src/systems/FXSystem.js</files>
  <action>
    1. Create a `SpriteNodeMaterial` for smoke particles.
    2. Implement the "Soft Particle" logic in the fragment stage using TSL:
       - Sample scene depth using `depth` node.
       - Calculate difference between fragment depth and scene depth.
       - Use `smoothstep` to fade alpha when the particle intersects geometry.
    3. Add a noise-driven alpha mask to the sprite for a more volumetric look.
    4. Set `depthWrite: false` and `transparent: true`. Use `AdditiveBlending` or standard transparency based on visual results.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Smoke particles blend softly with scene geometry and have a volumetric appearance.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke Event Integration</name>
  <files>src/systems/FXSystem.js</files>
  <action>
    1. Add a listener to `NetworkSystem` (or engine events) for `grenade:detonated` (with type 'smoke').
    2. Trigger the `computeSmokeSpawn` at the detonation position.
    3. Implement a mechanism to handle multiple active smoke clouds (up to a reasonable limit like 3-5).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Smoke effects are correctly triggered by grenade detonation events.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Volumetric Smoke with Soft Blending</what-built>
  <how-to-verify>
    1. Run the game in a WebGPU browser.
    2. Trigger a smoke grenade detonation (manually or via debug event).
    3. Verify that the smoke cloud expands and has a soft, volumetric look.
    4. Move the camera into the smoke and verify that it doesn't "clip" harshly with the floor or walls.
    5. Verify performance remains stable during the smoke bloom.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check for depth sampling artifacts and TSL build errors.
</verification>

<success_criteria>
- [ ] Smoke expands into a realistic volumetric cloud.
- [ ] Soft-particle blending eliminates hard edges against geometry.
- [ ] Multiple smoke grenades can be active simultaneously without crashing.
- [ ] Smoke simulation runs entirely on the GPU.
</success_criteria>

<output>
After completion, create `.planning/phases/06-fx-overhaul/06-02-SUMMARY.md`
</output>
