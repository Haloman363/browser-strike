---
phase: 09-strategic-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/GameState.js, src/systems/RoundSystem.js, src/systems/InputSystem.js, src/systems/WeaponSystem.js]
autonomous: true
requirements: [GAME-02, PERSIST, DROP]
must_haves:
  truths:
    - "Players receive $300 per kill"
    - "Players receive $3250 for a round win and $1400 for a loss"
    - "Inventory persists across rounds if the player survives"
    - "Pressing 'G' drops the current weapon as a world pickup"
  artifacts:
    - path: "src/GameState.js"
      provides: "Explicit inventory and cash management"
    - path: "src/systems/RoundSystem.js"
      provides: "End-of-round reward logic and survival persistence"
  key_links:
    - from: "RoundSystem.onRoundWin"
      to: "GameState.set({ cash: ... })"
      via: "Reward calculation"
---

<objective>
Implement the core economy logic in GameState and RoundSystem, including kill/round rewards and survival-based inventory persistence. Add the manual weapon dropping mechanic.
</objective>

<execution_context>
@/home/jaymes/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-strategic-layer/09-CONTEXT.md
@.planning/phases/09-strategic-layer/09-RESEARCH.md
@src/GameState.js
@src/systems/RoundSystem.js
@src/Constants_v2.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Economy Logic & Survival Persistence</name>
  <files>src/GameState.js, src/systems/RoundSystem.js</files>
  <behavior>
    - GameState.cash should increase by $300 on 'kill' event.
    - RoundSystem should award $3250 to winners and $1400 to losers at POST_ROUND transition.
    - RoundSystem.resetRound should NOT clear the player's inventory if GameState.isPlayerDead is false.
    - GameState should manage an 'inventory' object: { 1: primary, 2: secondary, 3: knife, 4: grenade, 5: c4 }.
  </behavior>
  <action>
    - Update GameState.js:
      - Add `inventory: { 1: null, 2: 'GLOCK', 3: 'KNIFE', 4: [], 5: null }` to initial state.
      - Add helper methods to add/remove weapons from slots.
    - Update RoundSystem.js:
      - In `init()`, listen for `engine:kill` to award $300.
      - In `onRoundWin()`, calculate rewards for all players (if bots/multiplayer) or just the player.
      - In `resetRound()`, check `GameState.get('isPlayerDead')`. If alive, skip weapon reset. If dead, reset to starter pistol/knife.
  </action>
  <verify>
    <automated>npx vitest src/GameState.js src/systems/RoundSystem.js</automated>
  </verify>
  <done>Cash rewards are applied correctly, and inventory survives round end if alive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Manual Weapon Dropping ('G')</name>
  <files>src/systems/InputSystem.js, src/systems/WeaponSystem.js, src/Factory.js</files>
  <behavior>
    - Pressing 'G' should trigger a 'weapon:drop' event.
    - Dropping a weapon should remove it from GameState.inventory.
    - A physicalized pickup should spawn at the player's position with forward velocity.
    - The player should automatically switch to the next available weapon (knife if all else gone).
  </behavior>
  <action>
    - Update InputSystem.js: Add listener for 'KeyG'.
    - Update WeaponSystem.js: 
      - Implement `dropCurrentWeapon()` logic.
      - Call `Factory.createGunModel(weaponKey, true)` to create a world pickup.
      - Apply initial impulse to the pickup.
      - Update GameState inventory slot to null.
      - Switch to next slot.
  </action>
  <verify>
    <automated>npx vitest src/systems/WeaponSystem.test.js</automated>
  </verify>
  <done>Pressing 'G' drops the current weapon and spawns a pickup.</done>
</task>

</tasks>

<verification>
- Start a game.
- Kill a bot: Verify cash increases by $300.
- End round: Verify cash increases ($3250/1400).
- Survive round: Verify weapons remain in inventory.
- Die in round: Verify weapons are reset to starter gear.
- Press 'G': Verify current weapon is dropped and can be picked up again.
</verification>

<success_criteria>
- Economy rewards match CS-style values.
- Inventory persistence works as intended.
- Weapon dropping is functional and physicalized.
</success_criteria>

<output>
After completion, create `.planning/phases/09-strategic-layer/09-01-SUMMARY.md`
</output>
