---
phase: 09-strategic-layer
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [src/systems/UISystem.js, style.css, src/systems/InputSystem.js]
autonomous: false
requirements: [GAME-02, ZONE]
must_haves:
  truths:
    - "Pressing 'B' opens/closes the Buy Menu SVG"
    - "Buy Menu is only accessible within the spawn buy zones"
    - "Players can purchase weapons using numeric keys (1-9) or mouse clicks"
    - "Insufficient funds prevent purchase and show a visual cue"
  artifacts:
    - path: "src/systems/UISystem.js"
      provides: "SVG Radial Buy Menu"
    - path: "style.css"
      provides: "Buy Menu layout and animations"
  key_links:
    - from: "UISystem.buyItem"
      to: "WeaponSystem.giveWeapon"
      via: "Method call"
    - from: "UISystem.checkBuyZone"
      to: "Maps_v2.spawnPoints"
      via: "AABB check"
---

<objective>
Implement the Radial Buy Menu UI in UISystem.js with SVG and CSS. Integrate with the economy logic and enforce buy zone restrictions.
</objective>

<execution_context>
@/home/jaymes/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-strategic-layer/09-CONTEXT.md
@.planning/phases/09-strategic-layer/09-RESEARCH.md
@src/systems/UISystem.js
@src/Constants_v2.js
@style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: SVG Radial Menu Structure & Styling</name>
  <files>src/systems/UISystem.js, style.css</files>
  <action>
    - Update UISystem.js:
      - Add `createBuyMenu()` method that generates an SVG radial wheel overlay.
      - Segments: Pistols, SMGs, Rifles, Heavy, Gear.
      - Use calculated SVG paths for arcs.
      - Add numeric labels (1-9) for each segment.
    - Update style.css:
      - Center the buy menu container.
      - Add hover effects for segments.
      - Add 'buy-available' and 'buy-disabled' styles.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create test/BuyMenuMock first</automated>
  </verify>
  <done>Radial SVG is rendered and looks correct when the menu is toggled.</done>
</task>

<task type="auto">
  <name>Task 2: Buy Zone Logic & Purchase Integration</name>
  <files>src/systems/UISystem.js, src/systems/InputSystem.js</files>
  <action>
    - Update UISystem.js:
      - Implement `checkBuyZone()`: Check if player position is within +/- 300 units of the team's spawn point (AABB).
      - Implement `openBuyMenu()` / `closeBuyMenu()` toggled by 'B' key.
      - Check buy zone AND round time (e.g. first 30s) before allowing 'B' to open.
      - Implement `purchaseItem(weaponKey)`:
        - Check `GameState.get('cash') >= WEAPONS_DATA[weaponKey].price`.
        - Deduct cash.
        - Call `WeaponSystem.giveWeapon(weaponKey)`.
        - Close menu.
    - Update InputSystem.js: Listen for 'KeyB'.
  </action>
  <verify>
    <automated>npm test src/systems/UISystem.test.js</automated>
  </verify>
  <done>Buying works within the zone, and funds are deducted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Tactical Economy and Buy Menu</what-built>
  <how-to-verify>
    - Open game at Dust2.
    - Press 'B' at spawn: Radial menu should appear.
    - Buy a Desert Eagle (700): verify 100 remaining.
    - Move away from spawn, press 'B': menu should NOT open.
    - Kill a bot: verify $300 reward.
    - Survive round: verify weapon persists.
    - Press 'G': verify weapon is dropped.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- Verify 'B' toggles menu only in buy zone.
- Verify SVG segments are responsive to mouse/keys.
- Verify cash is deducted correctly.
- Verify weapon is equipped immediately after purchase.
</verification>

<success_criteria>
- Radial menu is visually polished and functional.
- Buy zones are enforced based on spawn points.
- Integration with GameState cash and inventory is seamless.
</success_criteria>

<output>
After completion, create `.planning/phases/09-strategic-layer/09-02-SUMMARY.md`
</output>
